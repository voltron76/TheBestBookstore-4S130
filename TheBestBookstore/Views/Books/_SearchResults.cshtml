@model List<TheBestBookstore.Models.Book>

@if(Model.Count == 0)
{
    <p>No matching books found.</p>
}
else
{
    <ul>
        @foreach(var book in Model)
        {
            <li>@book.Title (ISBN: @book.ISBN)</li>
        }
    </ul>
}
